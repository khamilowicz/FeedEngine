= form_for [feed, post], url: feed_posts_path(feed.subdomain) do |f|
  = f.label :title
  = f.text_field :title
  = f.label :photo
  = f.text_field :photo
  = f.label :description
  = f.text_area :description
  = f.hidden_field :type, {value: post.class}
  = f.submit "Submit"